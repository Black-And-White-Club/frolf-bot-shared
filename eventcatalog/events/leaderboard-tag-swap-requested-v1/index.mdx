---
id: leaderboard-tag-swap-requested-v1
name: Tag Swap Request
version: 1.0.0
summary: Request to swap tag numbers between users
owners:
  - frolf-bot-team
badges:
  - content: leaderboard
    backgroundColor: '#8b5cf6'
    textColor: '#ffffff'
  - content: v1
    backgroundColor: '#6366f1'
    textColor: '#ffffff'
schemaPath: schema.json
---

Request to swap tag numbers between users

## Topic

This event is published on the `leaderboard.tag.swap.requested.v1` topic via NATS JetStream.

## Schema

<SchemaViewer file="schema.json" />

## Producers & Consumers

Use the node graph above to see which services produce and consume this event.
